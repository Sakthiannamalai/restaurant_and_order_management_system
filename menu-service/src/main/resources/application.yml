server:
  port: 8083

spring:
  application:
    name: menu-service
  cloud:
    compatibility-verifier:
      enabled: false

  datasource:
    url: jdbc:postgresql://localhost:5432/restaurant_main
    username: restaurant_user
    password: restaurant_password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

logging:
  level:
    com.restaurant.menu: DEBUG

eureka:
  client:
    service-url:
      defaultZone: http://restaurant-eureka-service:8761/eureka/
    register-with-eureka: true
    fetch-registry: true